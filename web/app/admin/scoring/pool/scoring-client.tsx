'use client';

export default function PoolScoringClient() {
  return (
    <div>
      <h2>Pool</h2>
      <p className="kicker" style={{ marginTop: 6 }}>
        Next up: 8-ball / 9-ball round-robin match entry (with table numbers) + Run attempts.
      </p>
      <div className="card">Pool scoring UI not wired up yet in the mock.</div>
    </div>
  );
}
